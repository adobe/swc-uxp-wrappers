## Description

This is UXP wrapper for `@spectrum-web-components/{{ name }}` package

### Usage

```
yarn add @swc-uxp-wrappers/{{ name }}
```

Import the side effectful registration of `<{{> tagnamePartial}}>` via:

```
import '@swc-uxp-wrappers/{{ name }}/{{> tagnamePartial}}.js';
```

When looking to leverage the `{{className name}}` base class as a type and/or for extension purposes, do so via:

```
import { {{className name}} } from '@swc-uxp-wrappers/{{ name }}';
```

## Example

```html
<{{> tagnamePartial}}></{{> tagnamePartial}}>
```
