{{> copyrightPartial }}

import { unsafeCSS } from '@spectrum-web-components/base';
import swc{{className name}}Styles from '@swc-uxp-internal/{{ name }}/src/{{ name }}.css.js';

import uxp{{className name}}Styles from './uxp-{{ name }}.css.js';

const combined{{className name}}Styles = unsafeCSS(swc{{className name}}Styles.toString(), uxp{{className name}}Styles.toString());

export default combined{{className name}}Styles;
