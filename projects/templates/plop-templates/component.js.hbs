{{> copyrightPartial }}

import { {{className name}} } from '@swc-uxp-internal/{{name}}/src/{{className name}}.js';

import styles from './uxp-{{ name }}.css.js';

class {{> uxpClassNamePartial}} extends {{className name}} {
    static get styles() {
        // We are combining our styles to make all super class styles available along with the transitive dependent classes styles. 
        return [super.styles, styles];
    }
}

export { {{> uxpClassNamePartial}} as {{className name}} };
